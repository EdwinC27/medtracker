# MedTracker — runtime dependencies
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
jinja2>=3.1
python-multipart>=0.0.9
apscheduler>=3.10,<4

# PDF export (v3). Pure Python, no system libraries needed.
reportlab>=4.0,<5

# Windows desktop toasts (installed only on Windows)
winotify>=1.1.0; sys_platform == "win32"

# Tests
pytest>=8.0
httpx>=0.27
